interface:
  display_name: "SWARMS"
  short_description: "Create, run, and observe native SWARMS workflows."
  default_prompt: "Use $swarms to create a safe workflow contract, run the Rust coordinator, and observe it in swarms-ui."
