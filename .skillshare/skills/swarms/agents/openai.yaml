interface:
  display_name: "SWARMS runtime"
  short_description: "Run durable multi-agent workflows through SWARMS."
  default_prompt: "Use $swarms to create, validate and run this durable workflow through the native Rust SWARMS coordinator."