interface:
  display_name: "Direct agent delegation"
  short_description: "Delegate directly to coding agents without SWARMS runtime."
  default_prompt: "Use $multi-provider-agent-orchestration to delegate this bounded task directly to the appropriate agent without using the SWARMS runtime."