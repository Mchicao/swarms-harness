[package]
name = "swarms-runtime"
version = "0.1.0"
edition = "2021"
description = "Low-overhead deterministic SWARMS coordinator."
license = "MIT"

[[bin]]
name = "swarms-rs"
path = "src/main.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
